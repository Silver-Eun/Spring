<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.songjava.mvc.repository.BoardRepository">
    <select id="getList" parameterType="com.spring.songjava.mvc.parameter.BoardSearchParameter"
            resultType="com.spring.songjava.mvc.domain.Board">
        SELECT B.boardSeq,
        B.title,
        B.contents,
        B.regDate
        FROM BOARD B
        <where>
            <if test="@org.apache.commons.lang3.StringUtils@isNotEmpty(keyword)">
                AND B.title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY B.regDate DESC
    </select>

    <select id="get" parameterType="int"
            resultType="com.spring.songjava.mvc.domain.Board">
        SELECT B.boardSeq,
               B.title,
               B.contents,
               B.regDate
        FROM BOARD B
        WHERE B.boardSeq = #{boardSeq}
    </select>

    <insert id="save" parameterType="com.spring.songjava.mvc.parameter.BoardParameter" useGeneratedKeys="true"
            keyProperty="boardSeq">
        INSERT INTO BOARD
        (title,
         contents,
         regDate)
        VALUES (#{title},
                #{contents},
                NOW())
    </insert>

    <update id="update" parameterType="com.spring.songjava.mvc.domain.Board">
        UPDATE BOARD
        SET title    = #{title},
            contents = #{contents}
        WHERE boardSeq = #{boardSeq}
    </update>

    <delete id="delete" parameterType="int">
        DELETE
        FROM BOARD
        WHERE boardSeq = #{boardSeq}
    </delete>

    <insert id="saveList" parameterType="map">
        INSERT INTO BOARD
        (title,
        contents,
        regDate)
        values
        <foreach collection="boardList" item="board" separator=",">
            (
            #{board.title},
            #{board.contents},
            NOW()
            )
        </foreach>
    </insert>
</mapper>